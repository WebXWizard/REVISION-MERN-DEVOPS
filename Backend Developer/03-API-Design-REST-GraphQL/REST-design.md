# REST Design

REST uses resources and HTTP methods.

Example:

```txt
GET /api/tasks
POST /api/tasks
GET /api/tasks/:id
PATCH /api/tasks/:id
DELETE /api/tasks/:id
```

Good REST APIs use nouns, correct status codes, and consistent response shapes.

