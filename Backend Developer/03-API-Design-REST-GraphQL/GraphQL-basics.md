# GraphQL Basics

GraphQL lets clients ask for specific data.

Concepts:

- Schema
- Query
- Mutation
- Resolver
- Type

REST is simpler for many apps. GraphQL is useful when clients need flexible data selection.

