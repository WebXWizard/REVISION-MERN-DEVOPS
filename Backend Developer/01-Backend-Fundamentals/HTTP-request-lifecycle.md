# HTTP Request Lifecycle

Basic flow:

```txt
Client sends request -> Server receives -> Middleware runs -> Controller handles -> Database queried -> Response sent
```

HTTP includes:

- Method
- URL
- Headers
- Body
- Status code
- Response body

