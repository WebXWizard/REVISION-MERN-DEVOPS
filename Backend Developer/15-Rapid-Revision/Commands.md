# Commands

Node:

```bash
npm init -y
npm install express dotenv
npm run dev
```

Docker:

```bash
docker build -t api .
docker run -p 3000:3000 api
```

SQL:

```sql
SELECT * FROM users;
CREATE INDEX idx_users_email ON users(email);
```

