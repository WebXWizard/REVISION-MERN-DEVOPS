# JWT, Sessions, And OAuth

JWT:

- Signed token carrying claims.

Session:

- Server-side auth state with session ID.

OAuth:

- Delegated authorization flow.

Choose based on app needs, security model, and infrastructure.

