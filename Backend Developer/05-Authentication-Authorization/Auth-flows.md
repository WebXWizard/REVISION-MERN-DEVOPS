# Auth Flows

Common flow:

```txt
Register -> Hash password -> Login -> Verify password -> Create session or token -> Access protected routes
```

Backend must enforce authorization checks, not just frontend UI.

