# Environment And Migrations

Environment variables:

- `PORT`
- `DATABASE_URL`
- `JWT_SECRET`
- `NODE_ENV`

Migrations change database schema safely.

Deployment should handle migrations carefully, especially for production data.

