# Docker Deployment

Docker packages backend apps.

Good Dockerfile:

- Uses small base image
- Installs only needed dependencies
- Does not include secrets
- Uses environment variables
- Exposes correct port

Run health route after deployment.

