# Secrets And CORS

Secrets:

- Store in environment or secret manager.
- Never commit to Git.
- Rotate if leaked.

CORS:

- Controls browser cross-origin API calls.
- Use exact frontend origin when credentials are enabled.

