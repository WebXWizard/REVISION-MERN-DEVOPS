# Docker

Docker packages applications into containers.

Core concepts:

- Image
- Container
- Dockerfile
- Registry
- Volume
- Network

Common commands:

```bash
docker build -t app .
docker run -p 3000:3000 app
docker ps
docker logs container-id
```

