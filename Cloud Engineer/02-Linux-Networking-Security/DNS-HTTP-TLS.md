# DNS, HTTP, And TLS

DNS:

- Converts domain names to IP addresses.

HTTP:

- Protocol used by web applications.

TLS:

- Encrypts traffic and validates identity using certificates.

Common issues:

- Wrong DNS record
- Expired certificate
- Port blocked
- Load balancer target unhealthy
- HTTP 502, 503, or 504 errors

