# Linux Basics

Useful commands:

```bash
pwd
ls -la
cd /var/log
cat file.txt
tail -f /var/log/syslog
ps aux
top
df -h
free -h
systemctl status nginx
```

Cloud use cases:

- Connect to servers using SSH.
- Check logs.
- Restart services.
- Debug disk, CPU, and memory issues.
- Install packages.

