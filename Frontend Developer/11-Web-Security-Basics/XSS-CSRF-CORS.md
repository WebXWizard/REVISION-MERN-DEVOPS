# XSS, CSRF, And CORS

XSS:

- Attacker runs script in user's browser.

CSRF:

- Attacker tricks browser into sending authenticated request.

CORS:

- Browser security rule controlling cross-origin requests.

Frontend security needs backend support too.

