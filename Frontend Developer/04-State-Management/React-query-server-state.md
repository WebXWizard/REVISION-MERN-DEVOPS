# React Query And Server State

Server state comes from API or backend.

React Query helps with:

- Caching
- Loading states
- Error states
- Refetching
- Mutations
- Query invalidation

Use React Query to avoid too much manual `useEffect` API logic.

