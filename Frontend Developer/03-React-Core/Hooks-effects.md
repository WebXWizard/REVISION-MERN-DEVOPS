# Hooks And Effects

Common hooks:

- `useState`
- `useEffect`
- `useRef`
- `useMemo`
- `useCallback`
- `useContext`

Use `useEffect` for side effects such as API calls, subscriptions, and syncing external systems.

Avoid unnecessary effects when derived values can be calculated during render.

