# Server And Client Components

Server components:

- Render on server.
- Can fetch data securely.
- Cannot use browser-only hooks.

Client components:

- Use state, effects, and browser events.

Use `"use client"` only when interactivity is needed.

