# Commands

Vite:

```bash
npm create vite@latest
npm install
npm run dev
npm run build
```

Next.js:

```bash
npx create-next-app@latest
npm run dev
npm run build
```

Git:

```bash
git status
git add .
git commit -m "message"
git push origin main
```

