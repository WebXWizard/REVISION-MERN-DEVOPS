# Debugging And DevTools

Use DevTools for:

- Console errors
- Network requests
- DOM inspection
- Performance profiling
- Storage inspection

React DevTools helps inspect components, props, state, and renders.

Debug method:

```txt
Reproduce -> Inspect -> Isolate -> Fix -> Test
```

