# CSS Layout

Core layout tools:

- Box model
- Flexbox
- Grid
- Positioning
- Media queries

Flexbox is best for one-dimensional layout.

Grid is best for two-dimensional layout.

Use `gap` instead of margin hacks when possible.

