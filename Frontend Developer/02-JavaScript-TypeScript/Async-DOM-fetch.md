# Async, DOM, And Fetch

Async concepts:

- Callback
- Promise
- Async await
- Event loop

DOM:

- `querySelector`
- `addEventListener`
- `classList`

Fetch:

```js
const res = await fetch("/api/products");
const data = await res.json();
```

Handle loading and errors.

