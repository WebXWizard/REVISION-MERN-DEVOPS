# kubectl Debugging

Useful commands:

```bash
kubectl get pods
kubectl describe pod pod-name
kubectl logs pod-name
kubectl logs pod-name --previous
kubectl get events
kubectl rollout undo deployment/app
```

Check status, events, logs, probes, resources, and recent changes.

