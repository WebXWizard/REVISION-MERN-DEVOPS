# Registry And Tagging

Registry stores images.

Examples:

- Docker Hub
- AWS ECR
- Azure ACR
- Google Artifact Registry

Good tags:

- Build ID
- Git SHA
- Semantic version

Avoid only using `latest` for production.

