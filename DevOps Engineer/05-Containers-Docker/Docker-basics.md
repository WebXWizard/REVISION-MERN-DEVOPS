# Docker Basics

Core concepts:

- Image
- Container
- Dockerfile
- Volume
- Network
- Registry

Useful commands:

```bash
docker build -t app .
docker run -p 3000:3000 app
docker ps
docker logs container-id
```

