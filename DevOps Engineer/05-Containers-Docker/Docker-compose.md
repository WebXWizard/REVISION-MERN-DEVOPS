# Docker Compose

Docker Compose runs multi-container apps.

Use for:

- App plus database
- Local development
- Test environments

Common file:

```txt
docker-compose.yml
```

Compose can define services, networks, volumes, and environment variables.

