# Git Basics

Useful commands:

```bash
git status
git branch
git checkout -b feature/name
git add .
git commit -m "message"
git push origin feature/name
```

Git tracks changes and enables versioned collaboration.

