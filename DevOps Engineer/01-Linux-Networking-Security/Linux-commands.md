# Linux Commands

Useful commands:

```bash
pwd
ls -la
ps aux
top
df -h
free -h
systemctl status nginx
journalctl -u nginx
tail -f /var/log/syslog
```

Check CPU, memory, disk, service status, and logs first.

