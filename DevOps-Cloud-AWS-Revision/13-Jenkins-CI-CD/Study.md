# Jenkins CI CD Study

Jenkins automates build, test, and deployment pipelines.

Important topics:

- Pipeline
- Jenkinsfile
- Declarative syntax
- Git webhooks
- Agents
- Credentials
- Stages

Mini task:

- Create pipeline: checkout, install, test, build Docker image.

