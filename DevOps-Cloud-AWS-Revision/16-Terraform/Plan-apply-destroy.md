# Plan Apply Destroy

Important commands:

```bash
terraform plan
terraform apply
terraform destroy
```

Meaning:

- `plan`: preview changes
- `apply`: create/update resources
- `destroy`: delete resources

Mini task:

- Explain safe Terraform workflow.

