# IAM Roles

IAM role is an identity that AWS services or users can assume temporarily.

Why used:

- Give EC2/Lambda access without storing access keys.

Real-world example:

- EC2 role allowing app to upload files to S3.

Mini task:

- Explain why IAM role is safer than hardcoded AWS keys.

