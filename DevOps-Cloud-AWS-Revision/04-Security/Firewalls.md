# Firewalls

Firewall controls incoming and outgoing traffic.

Why used:

- Block unwanted access.
- Allow only required ports.

Real-world example:

- Allow port 22 for SSH, 80 for HTTP, 443 for HTTPS.

Mini task:

- Write allowed ports for a MERN production server.

