# Least Privilege

Least privilege means user/service ko sirf required permissions dena.

Why used:

- Damage limit hota hai if account/key compromised.

AWS example:

- App that only reads S3 should not have admin access.

Mini task:

- Write minimum permissions for app uploading files to S3.

