# DNS

DNS domain name ko IP address me convert karta hai.

Example:

```txt
example.com -> 93.184.216.34
```

Why used:

- Humans names use karte hain, machines IP use karti hain.

Commands:

```bash
nslookup example.com
dig example.com
```

Mini task:

- Check DNS record of any website.

