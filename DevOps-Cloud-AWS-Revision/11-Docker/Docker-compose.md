# Docker Compose

Docker Compose runs multi-container apps using YAML file.

Why used:

- Define app, database, cache together.

Example services:

- frontend
- backend
- mongodb

Mini task:

- Design Compose services for MERN app.

