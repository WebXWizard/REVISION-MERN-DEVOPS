# Container Networking

Docker networking allows containers to communicate.

Common networks:

- bridge
- host
- none

Real-world example:

- Node API container connects to MongoDB container using service name in Docker Compose.

Mini task:

- Explain how API container talks to DB container.

