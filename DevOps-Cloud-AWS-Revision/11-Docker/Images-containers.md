# Images and Containers

Image is blueprint. Container is running instance of image.

Interview answer:

```txt
Image is immutable package. Container is runtime process created from image.
```

Mini task:

- Build image and run two containers from it.

