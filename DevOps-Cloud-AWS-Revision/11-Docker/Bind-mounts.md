# Bind Mounts

Bind mount maps host path into container.

Why used:

- Local development live code changes.

Difference:

```txt
Volume: managed by Docker
Bind mount: host path controlled by user
```

Mini task:

- Mount local app folder into Node container.

