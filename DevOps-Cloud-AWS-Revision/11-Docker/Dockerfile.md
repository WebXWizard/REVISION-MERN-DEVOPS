# Dockerfile

Dockerfile contains instructions to build Docker image.

Example instructions:

- `FROM`
- `WORKDIR`
- `COPY`
- `RUN`
- `EXPOSE`
- `CMD`

Common mistakes:

- Copying `node_modules`.
- Not using `.dockerignore`.
- Running as root unnecessarily.

Mini task:

- Write Dockerfile for Express app.

