# IAM Role For EC2

EC2 role gives instance temporary AWS permissions.

Why used:

- Avoid storing access keys on server.

Example:

- EC2 app uploads files to S3 using attached IAM role.

Mini task:

- Explain why IAM role is recommended for EC2 apps.

