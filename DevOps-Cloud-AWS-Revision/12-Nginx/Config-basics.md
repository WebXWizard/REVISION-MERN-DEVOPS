# Config Basics

Nginx config usually lives under `/etc/nginx`.

Important blocks:

- `server`
- `location`
- `upstream`

Common commands:

```bash
nginx -t
systemctl reload nginx
```

Mini task:

- Explain why `nginx -t` is used before reload.

