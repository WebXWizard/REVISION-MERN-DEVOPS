# os Module

`os` module interacts with operating system.

Use cases:

- Read environment variables
- Work with paths
- List files
- Create directories

Example:

```python
import os
print(os.environ.get("PORT"))
```

Mini task:

- Print all files in current directory.

