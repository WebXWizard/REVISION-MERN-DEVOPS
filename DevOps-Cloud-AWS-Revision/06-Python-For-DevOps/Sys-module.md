# sys Module

`sys` module provides access to Python runtime information.

Use cases:

- Command line arguments
- Exit codes

Example:

```python
import sys
print(sys.argv)
```

Mini task:

- Create script that accepts filename as argument.

