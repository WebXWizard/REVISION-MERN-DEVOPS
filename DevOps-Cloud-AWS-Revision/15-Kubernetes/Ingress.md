# Ingress

Ingress manages external HTTP/HTTPS access to services.

Why used:

- Route domains/paths to services.
- Centralize HTTP routing.

Example:

- `/api` goes to backend service.
- `/` goes to frontend service.

Mini task:

- Explain Ingress vs Service.

