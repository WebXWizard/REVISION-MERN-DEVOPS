# Dockerize Node App

Steps:

```txt
Create Dockerfile -> add .dockerignore -> build image -> run container -> test endpoint
```

Interview focus:

- Image size
- Environment variables
- Port mapping
- Logs

Mini task:

- Explain Dockerfile line by line.

