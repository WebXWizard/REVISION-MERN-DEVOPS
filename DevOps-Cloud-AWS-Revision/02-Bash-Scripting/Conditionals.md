# Conditionals

Conditionals run commands based on checks.

Example:

```bash
if [ -f app.log ]; then
  echo "log exists"
else
  echo "missing"
fi
```

Use cases:

- File exists check
- Service health check
- Disk usage alert

Mini task:

- Check if a directory exists, otherwise create it.

