# Variables

Bash variables store values.

Example:

```bash
APP_NAME="api"
echo "$APP_NAME"
```

Important:

- No spaces around `=`.
- Use quotes around variables.

Common mistakes:

- Writing `APP = "api"`.
- Not quoting variables with spaces.

Mini task:

- Write a script that prints app name and environment.

