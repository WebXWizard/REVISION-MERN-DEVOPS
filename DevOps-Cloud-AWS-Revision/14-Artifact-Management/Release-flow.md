# Release Flow

Basic release flow:

```txt
Code push -> CI build -> tests -> artifact/image -> registry -> deploy -> monitor
```

Why used:

- Repeatable and traceable deployments.

Mini task:

- Draw release flow for Dockerized Node API.

