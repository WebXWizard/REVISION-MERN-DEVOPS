# Package Management

Package managers install and update software.

Ubuntu/Debian:

```bash
apt update
apt install nginx
apt remove nginx
```

RHEL/CentOS:

```bash
yum install nginx
dnf install nginx
```

Common mistakes:

- Installing without updating package index.
- Not checking service after install.

Mini task:

- Install and check status of Nginx on Ubuntu.

