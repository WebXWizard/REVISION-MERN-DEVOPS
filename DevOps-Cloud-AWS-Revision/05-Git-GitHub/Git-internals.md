# Git Internals

Git stores snapshots of files, not just differences.

Important concepts:

- Working directory
- Staging area
- Local repository
- Remote repository
- Commit hash

Mini task:

- Explain what happens after `git add` and `git commit`.

