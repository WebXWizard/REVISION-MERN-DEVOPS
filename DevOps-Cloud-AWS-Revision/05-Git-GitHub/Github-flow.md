# GitHub Flow

GitHub Flow is simpler workflow.

Flow:

```txt
Create branch -> commit -> open PR -> review -> merge -> deploy
```

Why used:

- Good for continuous deployment.

Mini task:

- Compare Git Flow and GitHub Flow.

