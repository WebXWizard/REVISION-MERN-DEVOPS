# Conflicts

Merge conflict happens when Git cannot automatically combine changes.

How to resolve:

```txt
Open conflict file -> choose correct code -> remove markers -> test -> commit
```

Common mistakes:

- Removing useful code accidentally.

Mini task:

- Create and resolve a fake conflict.

