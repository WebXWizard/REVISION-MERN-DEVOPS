# Availability And Redundancy

Availability means the service is usable when users need it.

Redundancy means having extra components so one failure does not take down the whole system.

Examples:

- Multiple app replicas
- Multi-AZ database
- Load balancer
- Backup DNS strategy
- Read replicas

Important point:

```txt
Redundancy only helps when failover is tested.
```

