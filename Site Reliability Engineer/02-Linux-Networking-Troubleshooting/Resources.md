# Resources

Useful docs:

- Linux `man` pages
- Nginx documentation
- Curl documentation
- DNS basics from Cloudflare Learning Center
- HTTP status code reference

Practice:

- Break a local Nginx config and fix it.
- Run a small web server and inspect open ports.
- Use `curl`, `dig`, and `ss` on real domains.
- Write one runbook for a failing service.

