# Helm And Manifests

Kubernetes manifests:

- YAML files defining Kubernetes resources.

Helm:

- Package manager for Kubernetes.
- Uses charts and values files.

Use Helm for:

- Reusable deployments
- Environment-specific values
- Versioned releases
- Easier rollback

