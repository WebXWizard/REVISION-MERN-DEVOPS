# Build Artifacts

Artifacts are files produced by a build.

Examples:

- Compiled app
- Zip package
- Test report
- Docker image metadata
- Deployment manifest

Why publish artifacts:

- Reuse same build for deployment.
- Keep build and release traceability.
- Avoid rebuilding differently in each environment.

