# Scheduling And Dependencies

Scheduling runs workflows at specific times.

Dependencies define order.

Examples:

```txt
Create resource group -> Create network -> Create VM -> Configure monitoring
```

Avoid hidden dependencies. Make workflow order explicit.

