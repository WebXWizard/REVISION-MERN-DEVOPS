# Buffer

Buffer binary data handle karne ke liye use hota hai.

Why used:

- Files, network packets, streams.

Example:

```js
const buf = Buffer.from("hello");
```

Common mistakes:

- Buffer ko normal string samajhna.

Interview questions:

- What is Buffer?
- Why Node.js needs Buffer?

Mini task:

- Convert string to buffer and back.

