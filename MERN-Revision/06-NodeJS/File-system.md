# File System

`fs` module files read/write karne ke liye use hota hai.

Common methods:

- `readFile`
- `writeFile`
- `appendFile`
- `createReadStream`
- `createWriteStream`

Why used:

- Logs, uploads, config files, scripts.

Common mistakes:

- Sync methods use karna in request flow.

Interview questions:

- Async vs sync file methods?
- When to use streams?

Mini task:

- Read file and count words.

