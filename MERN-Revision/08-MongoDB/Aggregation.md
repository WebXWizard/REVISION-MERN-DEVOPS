# Aggregation

Aggregation data processing pipeline hai.

Common stages:

- `$match`
- `$group`
- `$sort`
- `$project`
- `$lookup`

Why used:

- Analytics, reports, grouped data.

Real-world example:

- Total sales by month.

Interview questions:

- What is aggregation pipeline?
- `$match` vs `$group`?

Mini task:

- Group orders by user and calculate total.

