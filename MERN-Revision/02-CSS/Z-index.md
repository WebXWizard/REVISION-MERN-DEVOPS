# z-index

`z-index` stacking order control karta hai.

Why used:

- Modal, dropdown, tooltip, overlay.

Important:

- Usually positioned element par work karta hai.
- Stacking context matter karta hai.

Common mistakes:

- `z-index` high karna but parent stacking context ignore karna.
- Position property missing.

Interview questions:

- Why z-index not working?
- What is stacking context?

Mini task:

- Create modal overlay above page content.

