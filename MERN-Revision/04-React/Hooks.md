# Hooks

Hooks functional components me React features use karne ke functions hain.

Common hooks:

- `useState`
- `useEffect`
- `useMemo`
- `useCallback`
- `useRef`
- `useContext`

Rules:

- Hooks top level par call karo.
- Hooks sirf React function component/custom hook me call karo.

Common mistakes:

- Condition ke andar hook call karna.

Interview questions:

- What are hooks?
- Rules of hooks?

Mini task:

- Create custom hook `useToggle`.

