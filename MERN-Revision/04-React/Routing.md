# Routing

Routing app ke different pages/views manage karta hai.

React Router concepts:

- `BrowserRouter`
- `Routes`
- `Route`
- `Link`
- `useNavigate`
- Dynamic params

Real-world example:

- `/products`, `/products/:id`, `/login`.

Common mistakes:

- Anchor tag use karke full reload karna.
- Protected route properly check na karna.

Interview questions:

- What is client-side routing?
- How do protected routes work?

Mini task:

- Create protected dashboard route.

