# Objects

Object key-value pairs store karta hai.

Why used:

- User profile, product data, configuration.

Example:

```js
const user = {
  name: "Amit",
  role: "MERN Developer"
};
```

Internal working:

- Objects reference type hote hain and heap memory me store hote hain.

Common mistakes:

- Dynamic key ke liye dot notation use karna.

Mini task:

- Create a user object and update one property immutably.

