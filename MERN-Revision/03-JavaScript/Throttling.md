# Throttling

Throttling function ko fixed interval me limited calls allow karta hai.

Why used:

- Scroll event
- Mouse move event
- Window resize

Example:

- Scroll handler max once every 300ms run ho.

Common mistakes:

- Debouncing and throttling confuse karna.

Interview questions:

- What is throttling?
- Where would you use throttling?

Mini task:

- Implement throttle for scroll event.

