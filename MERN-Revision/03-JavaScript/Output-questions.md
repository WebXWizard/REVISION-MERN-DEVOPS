# Output Questions

Practice these daily.

Question 1:

```js
console.log(a);
var a = 10;
```

Question 2:

```js
console.log(typeof null);
console.log(typeof []);
```

Question 3:

```js
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 1000);
}
```

Question 4:

```js
Promise.resolve().then(() => console.log("promise"));
setTimeout(() => console.log("timeout"), 0);
console.log("sync");
```

Mini task:

- Write answer with reason, not only output.

