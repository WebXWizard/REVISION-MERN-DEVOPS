# JWT

JWT token user identity claims carry karta hai.

Parts:

- Header
- Payload
- Signature

Express use:

- Login route token generate karta hai.
- Auth middleware token verify karta hai.

Common mistakes:

- Secret weak rakhna.
- Token expiry na dena.

Interview questions:

- What is JWT?
- JWT flow in Express?

Mini task:

- Create and verify JWT in middleware.

