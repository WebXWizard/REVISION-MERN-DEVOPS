# DNS, TLS, And VPN

DNS:

- Resolves names to IP addresses.

TLS:

- Encrypts traffic and verifies identity with certificates.

VPN:

- Creates encrypted network connectivity.

Common issues:

- Expired certificates
- Weak TLS versions
- Public DNS misconfiguration
- Overly broad VPN access

