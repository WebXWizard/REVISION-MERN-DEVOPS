# Linux Basics

Useful commands:

```bash
pwd
ls -la
ps aux
top
df -h
free -h
systemctl status service
journalctl -u service
tail -f /var/log/syslog
```

Platform use cases:

- Debug self-hosted agents
- Inspect container nodes
- Check logs and services
- Troubleshoot disk and memory issues

