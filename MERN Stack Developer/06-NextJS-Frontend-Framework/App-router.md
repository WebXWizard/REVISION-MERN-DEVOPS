# App Router

The App Router uses the `app` directory.

Common files:

- `page.tsx`
- `layout.tsx`
- `loading.tsx`
- `error.tsx`
- `not-found.tsx`

Benefits:

- Nested layouts
- Server components
- Streaming support
- Cleaner route organization

