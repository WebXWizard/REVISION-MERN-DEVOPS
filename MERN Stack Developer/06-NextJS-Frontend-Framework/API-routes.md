# API Routes

Next.js can create backend route handlers.

Use cases:

- Lightweight APIs
- Form submissions
- Auth callbacks
- Server-side data processing

For large backend systems, a separate Express API can still be better.

MERN note:

```txt
Next.js can be frontend only or can include backend route handlers depending on project design.
```

