# Server And Client Components

Server components:

- Render on the server.
- Can fetch data securely.
- Cannot use browser-only hooks.

Client components:

- Run in the browser.
- Can use state, effects, and events.

Use `"use client"` when a component needs interactivity.

