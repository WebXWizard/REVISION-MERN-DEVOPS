# Components, Props, And State

Component:

- Reusable UI function.

Props:

- Data passed from parent to child.

State:

- Data managed inside a component.

Simple rule:

```txt
Props are read-only input. State is local changing data.
```

