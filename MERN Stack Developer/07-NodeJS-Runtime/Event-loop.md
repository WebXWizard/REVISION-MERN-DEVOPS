# Node Event Loop

The event loop allows Node.js to handle many I/O tasks without blocking.

Examples of async I/O:

- Database query
- HTTP request
- File read
- Timer

Interview line:

```txt
Node.js is single-threaded for JavaScript execution but uses non-blocking I/O to handle concurrency.
```

