# Environment And Process

Environment variables store configuration.

Examples:

- `PORT`
- `MONGO_URI`
- `JWT_SECRET`
- `NODE_ENV`

Use `.env` locally and secure environment settings in production.

Never commit secrets to Git.

