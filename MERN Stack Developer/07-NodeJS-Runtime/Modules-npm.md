# Modules And npm

Module systems:

- CommonJS: `require`
- ES Modules: `import`

npm is the Node package manager.

Important files:

- `package.json`
- `package-lock.json`

Good practice:

- Commit lock file.
- Avoid unused dependencies.
- Use scripts for common commands.

