# REST API Design

Common methods:

- `GET`
- `POST`
- `PUT`
- `PATCH`
- `DELETE`

Example routes:

```txt
GET /api/products
GET /api/products/:id
POST /api/products
PATCH /api/products/:id
DELETE /api/products/:id
```

Use nouns for resources and correct status codes.

