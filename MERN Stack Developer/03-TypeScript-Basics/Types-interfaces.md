# Types And Interfaces

Basic types:

- `string`
- `number`
- `boolean`
- `null`
- `undefined`
- arrays
- objects

Interface example:

```ts
interface User {
  id: string;
  name: string;
  email: string;
}
```

Use types to catch mistakes before runtime.

