# Error Boundaries

Error boundaries catch rendering errors in React components.

They help show fallback UI instead of crashing the full app.

Use for:

- Dashboard sections
- Complex widgets
- Third-party components

Note:

```txt
Error boundaries do not catch async errors inside event handlers automatically.
```

