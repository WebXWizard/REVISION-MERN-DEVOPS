# Pagination And Filtering

Pagination limits data returned per request.

Example:

```txt
GET /api/products?page=2&limit=20&category=mobile
```

Benefits:

- Faster responses
- Lower database load
- Better frontend performance

Also support:

- Search
- Sorting
- Filtering

