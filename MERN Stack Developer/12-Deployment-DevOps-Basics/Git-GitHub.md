# Git And GitHub

Useful commands:

```bash
git status
git checkout -b feature/name
git add .
git commit -m "message"
git push origin feature/name
```

Good workflow:

- Feature branch
- Pull request
- Code review
- Merge to main
- Deploy from main

