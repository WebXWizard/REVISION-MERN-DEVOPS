# Environment Variables

Environment variables store config.

Examples:

- `PORT`
- `MONGO_URI`
- `JWT_SECRET`
- `CLIENT_URL`
- `NODE_ENV`

Rules:

- Never commit `.env`.
- Use different values for dev and production.
- Keep secrets in deployment platform settings.

