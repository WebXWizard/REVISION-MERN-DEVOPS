# Docker And CI/CD

Docker packages apps into containers.

CI/CD automates build, test, and deployment.

Basic pipeline:

```txt
Git push -> Install deps -> Test -> Build -> Deploy
```

Docker is useful when:

- You want consistent environments.
- Backend has multiple dependencies.
- You deploy to cloud or Kubernetes later.

