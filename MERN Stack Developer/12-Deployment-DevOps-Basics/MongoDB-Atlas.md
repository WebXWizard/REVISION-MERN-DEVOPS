# MongoDB Atlas

MongoDB Atlas provides managed MongoDB hosting.

Setup:

- Create cluster
- Create database user
- Configure network access
- Copy connection string
- Store in `MONGO_URI`

Security:

- Use strong password.
- Restrict IP access where possible.
- Never commit connection string.

