# Role Based Access Control

RBAC gives permissions based on roles.

Example roles:

- user
- seller
- admin

Example checks:

```txt
Only admin can delete users.
Only seller can create products.
Only owner can update own profile.
```

Implement RBAC in middleware and backend checks, not only UI.

